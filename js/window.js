// Items container
var g_items = [];

// Translate location function
translate_location = function (type, slot) {
	switch (type) {
		case 'w' :
			switch (slot) {
				case 0 : return 
			}
		break;
	}
}